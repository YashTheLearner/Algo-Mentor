"# dsa-helper" 
 architecture 
src/
│   actions/            <- next js server action
├── app/                ← Next.js routes (UI + API)
├── components/         ← Reusable React UI
├── lib/                ← Shared infrastructure & utilities
├── services/           ← Business logic
├── types/              ← Shared TypeScript types
├── data/               ← Static question JSON
└── hooks/              ← React hooks